[user]
        name       = "{{ user.name }}"
        email      = "{{ user.email }}"
        signingkey = "{{ user.email }}"

[push]
        gpgSign = if-asked

[commit]
        gpgSign = true

[tag]
        gpgSign = true
